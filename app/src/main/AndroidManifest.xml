<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tictactoe">



        <!-- Multiplayer Activities -->
        <activity android:name=".tictactoegame.MultiplayerGameActivity" android:exported="false" />
        <activity android:name=".tictactoegame.JoinRoomActivity" android:exported="false" />
        <activity android:name=".tictactoegame.CreateRoomActivity" android:exported="false" />
        <activity android:name=".tictactoegame.OnlineRoomActivity" android:exported="false" />

        <!-- Settings Activity -->
        <activity android:name=".tictactoegame.SettingsActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SENDTO" />
                <data android:scheme="mailto" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- AI Game Activities -->
        <activity android:name=".tictactoegame.AiGameActivity" android:exported="true" />
        <activity android:name=".tictactoegame.AiChooseSymbolActivity" android:exported="true" />
        <activity android:name=".tictactoegame.AIGetPlayerNameActivity" android:exported="true" />

        <!-- Offline Game Activities -->
        <activity android:name=".tictactoegame.OfflineGameActivity" android:exported="true" />
        <activity android:name=".tictactoegame.ChooseSymbolActivity" android:exported="true" />
        <activity android:name=".tictactoegame.OfflineGetPlayersNamesActivity" android:exported="true" />

        <!-- Launcher Activity (Use main app theme instead of AppCompat) -->
        <activity
            android:name=".tictactoegame.OfflineGameMenuActivity"
            android:exported="true"
            android:theme="@style/Base.Theme.Tictactoe">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
